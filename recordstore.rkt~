#lang racket

(require "records.rkt")
;; A file to implement the record store features (including walking around and drawing everything)

(struct Record-Store (name grid axioms))

(define RS1
  (Record-Store
   "BBQP"
   '(((jazz "JAZZ1") #f (hip-hop "HH1") #f)
     ((jazz "JAZZ2") #f (hip-hop "HH2") #f))))